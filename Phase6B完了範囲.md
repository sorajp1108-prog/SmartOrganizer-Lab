# Phase 6B ファイル操作の実用化

1. ファイル一覧の右クリックメニュー: 開く、元の場所を開く、パスをコピー、コピー、移動、詳細。
2. 起動をまたぐUndo: ジャーナルから最新のCompleted操作を取得し、対象・日時・元・先を確認後に実行。
3. 詳細表示: 名前、パス、種類、拡張子、サイズ、作成・更新日時、属性、取得元、Zone.Identifier要約。
4. 複数ファイル操作: 選択項目を事前計画し、1件ずつ安全操作を実行。キャンセル後は未着手項目をCancelledとして扱う。
5. 個別結果一覧: 成功、スキップ、失敗、キャンセルと各ファイルのメッセージを表示。
6. 同名時のスキップ: 既存ファイルを変更せず、その項目だけスキップ。
7. 名前を変更して保存: `名前 (1).拡張子`から空きを探索。既存ファイルを上書きしない。

## 安全制約

- 上書きは引き続き禁止。
- 一括操作は各ファイル別のOperationRecordを使用。
- 名前変更候補は同じ一括計画内でも重複しない。
- Undoは最新のCompleted操作を再検証してから実行。

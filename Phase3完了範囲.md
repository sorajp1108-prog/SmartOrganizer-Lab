# Phase 3 安全な書き込み操作

- コピーと移動は必ずプレビュー後に明示確認
- 同名ファイルは上書き禁止
- コピー先へ直接書かず `.partial` へ書いてから公開
- SHA-256で公開前後を検証
- 移動はコピー検証後、元ファイルを再検証してから削除
- 操作履歴をJSONジャーナルへ原子的に保存
- キャンセル時は一時ファイルを除去
- コピーUndoは保存先ハッシュ一致時のみ削除
- 移動Undoは保存先ハッシュ一致かつ元位置が空いている時のみ戻す
- Running状態を復旧候補として列挙

## 意図的な制限

- 上書きなし
- フォルダー操作なし
- 複数ファイル一括操作なし
- 起動をまたぐ「直前Undo」UIは未接続。履歴データ自体は保存される
- Windows実機コンパイルと障害注入試験は未実施

# Phase 7D 後始末候補エンジン

## 基本原則

候補生成は仮想表示だけで、削除、移動、名前変更を自動実行しない。構成ブロック内部、システム領域、個別操作不可項目は候補分析前に除外する。

## 候補

- 一時ファイル: `.tmp`、`.temp`、`.bak`、`.old`、`.dmp`で7日以上更新なし。使用中確認を加点。
- 未完了ダウンロード: `.part`、`.partial`、`.crdownload`、`.download`、`.opdownload`で24時間以上更新なし。使用中確認を加点。
- ログ・キャッシュ: `.log`、`.trace`、`.etl`または`logs`/`cache`配下で30日以上更新なし。
- 旧バージョン: 同じ基底名と拡張子を持ち、解析可能な版番号が複数ある場合に最新候補以外を提示。
- 孤立ファイル: 古い小型`.lnk`を「参照先未確認」の低確度候補として提示。ショートカット解決を行っていないため断定しない。
- インストーラー: MSI/MSIX/AppX系、またはsetup/install系の名前・VersionInfoを持つEXEで30日以上更新なし。

## 表示

クイックアクセスに「片付け候補」を追加。既存の5列一覧は維持し、理由・確度・自動削除しない旨は詳細画面だけに表示する。

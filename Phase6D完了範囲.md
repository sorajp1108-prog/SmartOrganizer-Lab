# Phase 6D 大規模整理の実用化

8. 入手元情報は初期走査から分離し、表示ページ単位で遅延読み込み。初期値は「読み込み待ち」。
9. 重複候補は全体SHA-256を含むグループIDで束ね、一覧にグループIDを表示し、ViewModelにもDuplicateGroupsを公開。
10. 重複検査は簡易ハッシュ・全体ハッシュごとに処理件数を進捗表示。
11. 大きいファイルのしきい値を1MBから102400MBで変更し、設定へ保存。
12. 最近追加の日数を1日から365日で変更し、設定へ保存。
13. 初回検出日時をfirst-seen.jsonへ原子的に保存し、「最近追加」はファイル作成日時ではなくFirstSeenAtで判定。
14. 一覧は200件ずつ段階追加。次ページを追加したとき、そのページの入手元情報だけを低優先で取得。

## 制約
- 初回検出台帳はパス単位。移動・名前変更時の同一性追跡はFile ID導入まで未対応。
- 入手元遅延読み込み中に再検索すると旧処理をキャンセル。
- 重複グループは表示のみで自動削除しない。

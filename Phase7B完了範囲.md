# Phase 7B 二段階操作権限

## 一般利用者
- 初期値は「通常」。単体ファイルは従来どおり操作可能。
- アプリ、開発プロジェクト、用途不明の一式は1行の保護ブロックとして表示し、内部ファイルは表示しない。
- 保護対象を操作しようとしても、設定から高度なファイル操作を有効にする案内だけを表示。
- Windows、System32、SysWOW64、WindowsAppsは高度な設定でも拒否。

## パワーユーザー
- 三点メニューの「設定」から「高度なファイル操作」を有効化。
- 高度モードでは保護ブロックの内部ファイルを一覧へ追加表示。
- 操作前に対象と所属ブロックを確認し、「許可して続行」で今回だけ許可。
- 「この一式では今後確認しない」にチェックすると、安定したブロックIDを保存して以後の確認を省略。
- 設定では「毎回」「一式ごとに最初の1回」「確認しない」を選択可能。
- 保存済みの確認省略は設定から全消去可能。

## 維持する安全性
- 高度モードでもプレビュー、上書き禁止、SHA-256、操作履歴、Undo、コミット境界は維持。
- システム重要領域の拒否は解除不可。
- 自動実行は行わない。

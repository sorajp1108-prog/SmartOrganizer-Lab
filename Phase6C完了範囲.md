# Phase 6C 整理機能の強化

1. 監視フォルダー変更: FolderPicker、保存、次回復元、監視と問い合わせの切り替え。
2. クイックアクセス: 現在地、ダウンロード、デスクトップ、ドキュメント、最近追加、大きいファイル、重複候補。
3. 正式な取得元判定: `Zone.Identifier`のZoneId、HostUrl、ReferrerUrlを読み、確認できない場合は推測で断定しない。
4. サブフォルダー切り替え: 初期OFF。ON時のみ再帰走査し、相対フォルダーで絞り込み。
5. 最近追加: 作成日時が過去7日以内。日数は設定サービスで1から365日に制限。
6. 大きいファイル: 初期500MB以上。しきい値は設定サービスに保存可能。
7. 重複候補: まずサイズ一致、次に先頭末尾64KBの簡易SHA-256、最後に全体SHA-256が一致したファイルだけを候補化。

## 厳重な安全制約

- 重複候補は表示のみ。自動削除、自動移動、自動統合はしない。
- 0バイトファイルは重複候補から除外。
- 取得元情報がないファイルを特定のブラウザー由来と推測しない。
- サブフォルダーは利用者が明示的に有効化するまで走査しない。
- アクセス拒否や読み取り中の変更は、該当ファイルを飛ばし全体処理を継続する。
